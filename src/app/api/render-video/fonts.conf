<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
    <!-- This is a minimalist fontconfig file to prevent ffmpeg/fontconfig from erroring on Windows server environments -->
    <!-- where it cannot find a default system configuration. -->
    <cachedir>.</cachedir>
</fontconfig>
